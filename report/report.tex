\documentclass[12pt]{article}

% ---------- Packages ----------
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc} % if you use pdflatex
\usepackage[english]{babel}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{float}
\usepackage[hidelinks]{hyperref}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,fit,shapes.geometric}
\usetikzlibrary{decorations.pathreplacing}


\usepackage{titling} % for \pretitle, \posttitle, etc.
\newcommand{\HRule}{\rule{\linewidth}{0.6pt}}

\pretitle{\begin{center}\HRule\\[1.2ex]\LARGE}
\posttitle{\\[1.2ex]\HRule\end{center}}
\preauthor{\begin{center}\large}
\postauthor{\end{center}}
\predate{\begin{center}}
\postdate{\end{center}}

% ---------- Formatting ----------
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}
\setcounter{tocdepth}{2} % sections + subsections in TOC
\hypersetup{
  pdftitle={Parallel and Distributed Systems: Paradigms and Models - Project 1 (MergeSort) Report},
  pdfauthor={Davide Marchi},
  pdfsubject={Distributed out-of-core MergeSort},
  pdfcreator={LaTeX}
}

% ---------- Title ----------
\title{Parallel and Distributed Systems: Paradigms and Models\\
Project 1 (MergeSort) Report}
\author{Davide Marchi (602476)\\\texttt{d.marchi5@studenti.unipi.it}}
\date{August 2025}

\begin{document}
% No page numbers on title and ToC
\pagenumbering{gobble}

\maketitle
\begin{center}
Code repository:

\url{https://github.com/davide-marchi/Parallel-and-Distributed-Systems-Paradigms-and-Models}
\end{center}
\clearpage

\tableofcontents
\clearpage

% Start numbering (1) from here
\pagenumbering{arabic}
\setcounter{page}{1}


% =========================================================
\section{Introduction}
I present a complete study of a distributed out-of-core MergeSort. The study proceeds in four steps. First, I describe the problem and the data layout. Second, I implement four solutions that share a common I/O pipeline while differing in how they exploit parallelism: a sequential baseline, an OpenMP task based version, a FastFlow farm based version, and a multi node hybrid MPI solution that reuses the single node core. Third, I evaluate these versions under controlled workloads and hardware configurations, measuring runtime, speedup, and efficiency. Finally, I discuss the results and propose an approximate cost model for the distributed solution.

All implementations work on files that are potentially larger than available RAM. Each program first scans the unsorted file to build an in memory index of keys, offsets, and lengths, then sorts this index, then rewrites a new file in key order by copying each record from its recorded offset. To keep memory usage proportional to the number of records, the in memory structure stores only keys, offsets, and lengths. The write phase can be highly variable on a shared cluster, so the primary performance analysis focuses on the reading and sorting stages. The full pipeline remains functionally correct and the final output is validated.

The objectives are:
\begin{itemize}
  \item demonstrate correctness of all variants by verifying that keys are globally sorted in the \emph{written} output file and that records are preserved;
  \item quantify single node scalability with strong and weak scaling for OpenMP and FastFlow;
  \item quantify multi node scalability for the MPI hybrid, using one rank per node and varying threads per rank;
  \item interpret bottlenecks that arise from memory access, tasking overheads, and communication.
\end{itemize}

\textbf{Structure.}
Section~\ref{sec:problem} defines the problem and data model. Section 3 discusses memory and I/O considerations. Section 4 presents single node implementations and their evaluation. Section 5 presents the multi node implementation and its evaluation. Section 6 proposes an approximate cost model and discusses bottlenecks and overlap.

% =========================================================
\section{Problem description}
\label{sec:problem}

\subsection{Data model and constraints}
The input is a large binary file that stores an array of records. Each record consists of a sortable key, a length field, and a payload. The payload size can be large compared to the key, and the file may exceed available RAM, which motivates an out-of-core strategy.

I use the following logical layout for each \textbf{Record}:
\begin{itemize}
  \item \textbf{key}: 64 bit unsigned integer used for ordering;
  \item \textbf{len}: 32 bit unsigned integer that gives the payload length in bytes, with \(\text{len} \le P\) for a dataset parameter \(P\);
  \item \textbf{payload}: a byte array of length \(\text{len}\).
\end{itemize}

Sorting records in place is impractical when the file is larger than RAM. Instead, I construct an auxiliary in memory index. Each entry is an \textbf{IndexRec} that stores only what is needed to order and later locate the record:
\begin{itemize}
  \item \textbf{key}: 64 bit unsigned integer, copied from the record;
  \item \textbf{offset}: 64 bit unsigned integer, the byte offset where the record begins in the unsorted file;
  \item \textbf{len}: 32 bit unsigned integer, copied from the record, used to stream the right number of bytes during rewrite.
\end{itemize}
The index has length \(N\), one entry per record. Its memory footprint is \(O(N)\) and is independent of the maximum payload size \(P\). This design keeps memory predictable and minimizes disk traffic during random access.

Correctness means that the output file contains exactly the input records in nondecreasing key order. I validate this by scanning the \emph{written} output file from disk, checking that keys are ordered and that the record count matches the input.

\subsection{Pipeline, distributed setting, and metrics}
The pipeline is the same across all variants:
\begin{enumerate}
  \item \textbf{Index build.} Scan the unsorted file, read \texttt{key} and \texttt{len} per record, and fill the \texttt{IndexRec} array with \{key, offset, len\}. File access uses memory mapping to support efficient random access patterns and to avoid large user space buffers.
  \item \textbf{Index sort.} Sort the \texttt{IndexRec} array by key. The sequential baseline uses \texttt{std::sort}. The parallel versions use a task based mergesort with a base case threshold that delegates small subarrays to \texttt{std::sort}.
  \item \textbf{Rewrite.} Create the output file, then for each entry in sorted order fetch the record at \texttt{offset} from the unsorted file and copy the entire record to the output, i.e., \(\texttt{sizeof(key)}+\texttt{sizeof(len)}+\texttt{len}\) bytes, where \texttt{len} specifies the payload size.
  \item \textbf{Validation.} Scan the output file to confirm nondecreasing key order and that the record count is unchanged.
\end{enumerate}

In the multi node variant, I run one MPI rank per node. A designated rank reads the file and builds the global index, then partitions that index into contiguous slices and sends one slice to each rank. Each rank sorts its slice locally. The globally sorted order is obtained through a sequence of pairwise merges across ranks. The final rank rewrites the output file and performs validation. Communication is limited to index data and merged index segments, which keeps memory usage predictable and decoupled from payload size.

The primary metrics are total time for index build plus sort, speedup \(S_p = T_1 / T_p\), and efficiency \(E_p = S_p / p\). For single node studies, \(p\) is the thread count. For distributed studies, \(p\) denotes the number of nodes; when I vary threads per node, I plot separate curves for each thread count. Strong scaling holds \(N\) fixed while varying \(p\). Weak scaling grows \(N\) proportionally to \(p\).

% =========================================================
\section{Memory and I/O considerations}
My design keeps memory usage proportional to the number of records rather than to the maximum payload size or to an arbitrary buffer. I maintain an in memory index of length \(N\), where each entry stores only the information required to order and later locate a record: \(\{\texttt{key}, \texttt{offset}, \texttt{len}\}\). The footprint is therefore \(O(N)\) with a small constant factor that depends on the sizes of these fields, and it does not depend on the maximum payload parameter \(P\). During rewrite I stream each entire record directly from the unsorted file into the output without caching all payloads in RAM.

\subsection{Why I use memory mapping}
I use \texttt{mmap} for both reading and writing. This choice avoids explicit user space I/O buffers and lets the operating system handle paging and readahead. It is well suited to the two access patterns in this project:
\begin{itemize}
  \item During index build, I scan the file record by record and read only \texttt{key} and \texttt{len}. Mapping the file allows the kernel to fault in pages efficiently and to reuse them across passes when the page cache is warm.
  \item During rewrite, I must fetch records at the original positions and emit them in sorted order. This requires many random reads of the source and a sequential write to the destination. With \texttt{mmap} I can copy each entire record (header + payload) from the mapped source to the mapped destination without building large staging buffers.
\end{itemize}
This approach keeps per record work bounded and predictable, and it aligns with the constraint that the file may be much larger than available RAM.

\subsection{Single threaded disk access}
On the shared cluster, attempts to parallelize disk access did not produce stable gains. Running multiple threads that read or write concurrently tended to increase queue contention and amplify interference from other users on the same storage. I therefore adopt a conservative policy:
\begin{enumerate}
  \item I perform file I/O with a single thread at a time.
  \item I exclude the rewrite phase from the primary performance graphs and focus the analysis on index build plus sorting.
\end{enumerate}
This policy isolates the compute and indexing behavior of the implementations from storage noise and makes cross run comparisons meaningful.

\subsection{Variability of the write phase}
The write phase is highly volatile on the target system. In repeated executions of the same configuration, the time to rewrite the sorted file varied widely, by nearly an order of magnitude. Table~\ref{tab:write-variance} reports three representative extremes observed under identical settings.

\begin{table}[h]
  \centering
  \caption{Observed variability of the rewrite phase: representative extremes from repeated identical runs}
  \label{tab:write-variance}
  \begin{tabular}{@{}llll@{}}
    \toprule
    Run & Write time (s) & Notes \\
    \midrule
    1   & 68.97   & light system load \\
    2   & 264.69  & moderate contention \\
    3   & 503.60  & heavy contention \\
    \bottomrule
  \end{tabular}
\end{table}

Given this spread, including rewrite time in the main figures would mask the effects I want to study. I still execute the full pipeline in every run and I always validate correctness on the \emph{written} output file from disk, but I report index build plus sorting as the primary metric in Sections~4 and 5.

% =========================================================
\section{Single node implementations}

All single node variants share the same three stage pipeline: I first scan the file to build an in memory index of \(\{\texttt{key}, \texttt{offset}, \texttt{len}\}\) entries, then I sort this index, then I rewrite the output file by copying each record from the original position to its new position. Using the \texttt{IndexRec} vector keeps memory proportional to the number of records \(N\), independent of the maximum payload size \(P\). For the parallel variants I use a task based mergesort with a base case threshold of 10\,000 elements that falls back to \texttt{std::sort} to avoid unnecessary tasking overhead.

\subsection{Sequential baseline}
The sequential program performs no overlap. It scans the file and fills the \texttt{IndexRec} vector, sorts the entire vector with \texttt{std::sort}, then rewrites the output file by copying the whole record from the source mapping at \texttt{offset} with size \(\texttt{sizeof(key)}+\texttt{sizeof(len)}+\texttt{len}\). This run is my reference \(T_1\) for speedup.

\subsection{FastFlow implementation}
I use a single FastFlow farm. With \(n\) threads, the farm consists of one emitter and \(n-1\) workers.

\begin{itemize}
  \item \textbf{Single thread fallback.} If only one thread is available, I bypass the farm and call \texttt{std::sort} on the whole index.
  \item \textbf{Tasking strategy.} The emitter first schedules the index build as the initial task. It then generates all mergesort tasks in a recursive manner. When a subarray size reaches the base case of 10\,000, the corresponding leaf task is enqueued immediately and executed with \texttt{std::sort}.
  \item \textbf{Readiness gating.} To start sorting as soon as possible, leaf tasks coordinate with a \texttt{ProgressGate}. Each leaf knows its \([L, R)\) range of index entries and sleeps until the index builder signals that the required portion has been filled. The reading worker wakes up any waiting leaves every 10\,000 records in the best case.
\end{itemize}

This design allows the workers to overlap sorting of ready chunks with the ongoing index build. The emitter cost means that with \(n=2\) the farm has only one active worker, which directly affects that specific case in the results.

\subsection{OpenMP implementation}
I implement a task based top down mergesort over the \texttt{IndexRec} vector while the index is being built.

\begin{itemize}
  \item \textbf{Scheduling.} Inside a single OpenMP parallel region, I spawn one task that builds the index and, in parallel, I generate the mergesort task tree with \texttt{\#pragma omp task} and \texttt{taskgroup}. As in the FastFlow version, any subarray of size at most 10\,000 is handled by \texttt{std::sort}.
  \item \textbf{Readiness gating.} Leaf tasks check the \texttt{ProgressGate} to ensure the needed slice of the index has been produced before proceeding.
  \item \textbf{Single thread behavior.} When there is exactly one OpenMP thread, I enforce a \texttt{taskwait} after the index build task and \emph{before} spawning the merge tasks. This prevents the single available thread from being occupied by merge tasks that would block waiting for data the reader cannot produce concurrently.
\end{itemize}

\subsection{Performance evaluation}

I evaluate index build plus sort time for the sequential, FastFlow, and OpenMP implementations over multiple input sizes, payload maxima, and thread counts.

\subsubsection{Execution times}
I report the total time
\[
T \;=\; T_{\text{build-index}} \;+\; T_{\text{sort-index}} \, .
\]
This excludes the rewrite phase as discussed in Section~3.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/seq_omp_ff_time_3x3.png}
  \caption{Execution time \(T\) for index build plus sort across a \(3\times3\) grid of \((\text{records}, \text{payload}_{\max})\).}
  \label{fig:time-3x3}
\end{figure}

\subsubsection{Absolute speedup}
Absolute speedup is defined against the \emph{best sequential} time for the same dataset settings:
\[
S_{\text{abs}} \;=\; \frac{T_{\text{seq}}^{\ast}}{T_{\text{variant}}} \, ,
\]
where \(T_{\text{seq}}^{\ast}\) is the fastest observed sequential time for that \((N, \text{payload}_{\max})\) and \(T_{\text{variant}}\) is the time of the FastFlow or OpenMP run.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/seq_omp_ff_speedup_3x3.png}
  \caption{Absolute speedup \(S_{\text{abs}}\) relative to the best sequential time on the same \(3\times3\) grid.}
  \label{fig:speedup-3x3}
\end{figure}

\paragraph{Absolute efficiency.}
To normalize for the number of threads, I also report \emph{absolute efficiency}
\[
E_{\text{abs}} \;=\; \frac{S_{\text{abs}}}{p}
\;=\; \frac{T_{\text{seq}}^{\ast}}{p \cdot T_{\text{variant}}} \, ,
\]
where \(p\) is the thread count of the parallel run (for FastFlow, \(p\) is the total threads including the emitter). Values near 1 indicate near ideal utilization at that problem size; lower values reflect overheads or memory/IO bottlenecks.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/seq_omp_ff_efficiency_3x3.png}
  \caption{Absolute efficiency \(E_{\text{abs}} = S_{\text{abs}}/p\) on the same \(3\times3\) grid of \((\text{records}, \text{payload}_{\max})\).}
  \label{fig:abs-efficiency-3x3}
\end{figure}


\subsubsection{Strong scalability}
For strong scaling I fix \(N\) and vary the thread count \(p\). I use relative speedup with respect to the \emph{same} parallel implementation at one thread:
\[
S_{\text{strong}}(p) \;=\; \frac{T_{\text{par}}(1,N)}{T_{\text{par}}(p,N)} \, .
\]
The figure shows time, relative speedup, and efficiency panels for completeness.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/strong_scaling_time_speedup_efficiency.png}
  \caption{Strong scaling at \(N=100{,}000{,}000\) and \(\text{payload}_{\max}=32\): time, relative speedup \(S_{\text{strong}}(p)\), and efficiency.}
  \label{fig:strong-scaling}
\end{figure}

\subsubsection{Weak scalability}
For weak scaling I increase the number of elements with the thread count to reflect the \(O(N\log N)\) cost of sorting. I choose \(N_p\) so that \(N_p \log_2 N_p\) grows approximately linearly with \(p\), and I use relative speedup with respect to the parallel implementation at one thread:
\[
S_{\text{weak}}(p) \;=\; \frac{T_{\text{par}}(1, N_1)}{T_{\text{par}}(p, N_p)} \, .
\]

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/weak_scaling_time_speedup_efficiency.png}
  \caption{Weak scaling with \(N_p\) growing with \(p\) to reflect \(O(N\log N)\): time, relative speedup \(S_{\text{weak}}(p)\), and efficiency.}
  \label{fig:weak-scaling}
\end{figure}

\subsection{Observations}
The measurements support the following points.

\begin{itemize}
  \item \textbf{OpenMP with one thread.} The OpenMP version with a single thread is much slower than the sequential baseline. In this case I enforce a \texttt{taskwait} to ensure the reading task finishes before merging begins, which completely removes any overlap between reading and merging and likely contributes to the slowdown.
  \item \textbf{FastFlow with two threads.} With exactly two threads, one acts as emitter and only one remains as a worker, so this specific case cannot beat the sequential baseline. For four or more threads, the FastFlow version outperforms the sequential baseline.
  \item \textbf{Scaling with threads.} As I increase threads, both OpenMP and FastFlow outperform the sequential version. OpenMP is slightly better than FastFlow at higher thread counts due to lower runtime overheads.
  \item \textbf{Effect of problem size and payload.} Speedup grows with the number of elements and shrinks with larger payloads. Larger \(N\) increases the compute share in sorting, while larger payloads increase page faults and time spent building the index relative to sorting.
  \item \textbf{Memory bound cases.} With few elements and large payloads the execution tends to be memory bound. During index build, I use \texttt{mmap}, so reading one record faults in a whole page. Larger payloads reduce the number of keys per page, which increases the number of pages that need to be fetched and slows the index build.
\end{itemize}

% =========================================================
\section{Multi node implementations}

I implement a hybrid MPI plus OpenMP solution with one MPI process per node and a variable number of OpenMP threads per process. Only one node touches the disk at a time. I observed that overlapping disk activity across nodes did not improve performance on the shared Slurm cluster and that concurrent I/O from different jobs increased contention, so I serialize disk access.

\subsection{Implementation overview}
Rank 0 reads the unsorted file and builds the global \texttt{IndexRec} array. As soon as \(\lfloor N/P \rfloor\) entries are ready, where \(P\) is the number of nodes, rank 0 partitions the index into contiguous slices and starts sending them asynchronously with \texttt{MPI\_Isend}. Each receiver posts \texttt{MPI\_Recv}, sorts its local slice with OpenMP, then participates in a pairwise merge schedule. Merging proceeds in \(\lceil \log_2 P \rceil\) rounds. In round \(r\), rank \(i\) exchanges data with partner \(i \oplus 2^r\) when the partner exists. The receiver merges two sorted segments into one larger sorted segment. At the end of the last round, rank 0 holds the globally sorted index, rewrites the output file, and validates correctness on the written file. No node other than rank 0 writes the output file.

\subsection{Single node sorting}
On each node I sort the local slice with OpenMP tasks, using the same top down mergesort as in the single node section and the same base case of 10\,000 elements that falls back to \texttt{std::sort}. I keep the post-receive merge single threaded on the destination rank, both for simplicity and because each merge is short at the chunk sizes I use; the extra synchronization and cache traffic of a multithreaded merge would likely outweigh any benefit.


\subsection{Performance evaluation}
I analyze the MPI version in the balanced case with \(\text{payload}_{\max}=32\) bytes. The x axis is always the number of nodes. Lines in each plot denote different thread counts per node. There is exactly one MPI process per node. All times exclude the rewrite phase as discussed in Section~3.

\subsubsection{Execution times over nodes}
I report the total time
\[
T \;=\; T_{\text{build-index}} \;+\; T_{\text{sort-index}} \;+\; T_{\text{distributed-merges}} \, ,
\]
for several dataset sizes \(N\) at fixed \(\text{payload}_{\max}=32\). The three panels show small, medium, and large \(N\).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/mpi_time_varyN.png}
  \caption{Execution time \(T\) versus nodes at \(\text{payload}_{\max}=32\). Panels correspond to increasing \(N\). Lines are threads per node.}
  \label{fig:mpi-time-varyN}
\end{figure}

\subsubsection{Absolute speedup at fixed size}
Absolute speedup compares MPI performance to the best sequential baseline on the same dataset:
\[
S_{\text{abs}}(P,t) \;=\; \frac{T_{\text{seq}}^{\ast}(N)}{T_{\text{mpi}}(P,t,N)} \, ,
\]
where \(P\) is nodes, \(t\) is threads per node, and \(T_{\text{seq}}^{\ast}(N)\) is the fastest observed sequential time at size \(N\). I also report \emph{node efficiency}, normalized by the number of nodes:
\[
E_{\text{node}}(P,t) \;=\; \frac{S_{\text{abs}}(P,t)}{P} \, .
\]

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/mpi_abs_fixedN_time_speedup_efficiency.png}
  \caption{Fixed \(N\) and \(\text{payload}_{\max}=32\): time \(T\), absolute speedup \(S_{\text{abs}}\), and node efficiency \(E_{\text{node}}\) versus nodes. Lines are threads per node.}
  \label{fig:mpi-abs-fixedN}
\end{figure}

\subsubsection{Strong scalability}
For strong scaling I fix \(N=100{,}000{,}000\) and \(\text{payload}_{\max}=32\). I define relative speedup per thread count as
\[
S_{\text{strong}}(P; t) \;=\; \frac{T_{\text{mpi}}(1, t, N)}{T_{\text{mpi}}(P, t, N)} \, , \qquad
E_{\text{strong}}(P; t) \;=\; \frac{S_{\text{strong}}(P; t)}{P} \, .
\]
Each curve uses its own one node baseline at the same \(t\).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/mpi_strong_scaling_time_speedup_efficiency.png}
  \caption{Strong scaling at \(N=100{,}000{,}000\) and \(\text{payload}_{\max}=32\): time, relative speedup \(S_{\text{strong}}\), and efficiency \(E_{\text{strong}}\) versus nodes.}
  \label{fig:mpi-strong}
\end{figure}

\subsubsection{Weak scalability}
For weak scaling I increase the number of elements with the number of nodes to reflect the \(O(N \log N)\) cost of sorting. I choose sizes \(N_P\) so that \(N_P \log_2 N_P\) grows approximately linearly with \(P\). Relative speedup and efficiency are
\[
S_{\text{weak}}(P; t) \;=\; \frac{T_{\text{mpi}}(1, t, N_1)}{T_{\text{mpi}}(P, t, N_P)} \, , \qquad
E_{\text{weak}}(P; t) \;=\; \frac{S_{\text{weak}}(P; t)}{P} \, .
\]

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/mpi_weak_scaling_time_speedup_efficiency.png}
  \caption{Weak scaling with \(\text{payload}_{\max}=32\) and \(N_P\) chosen so that \(N_P \log_2 N_P\) scales with \(P\): time, relative speedup \(S_{\text{weak}}\), and efficiency \(E_{\text{weak}}\).}
  \label{fig:mpi-weak}
\end{figure}

\subsection{Observations}
The results show the following trends.

\begin{itemize}
  \item \textbf{Few threads per node.} With one or a few threads per node I gain by distributing across nodes. Communication costs are small relative to local sorting time, so adding nodes reduces time meaningfully.
  \item \textbf{Many threads per node.} As I increase threads per node, local sorting becomes faster and the communication share grows. Beyond a point, spreading work across nodes hurts. For example, with 32 threads per node the communication time outweighs the benefit of splitting work across 8 nodes, making the run communication bound.
  \item \textbf{Communication-bound crossover.} As on-node sorting speeds up, the fraction of time spent in communication grows. Message startup and bandwidth transfer for sending and receiving index segments dominate, so the execution becomes communication bound.
  \item \textbf{Pairwise merge underutilization.} The pairwise merge schedule inherently halves the number of active ranks each round, which leaves increasing fractions of the cluster idle during merging as \(P\) grows. This structural underutilization is visible as a decreasing efficiency with more nodes in the strong and weak scaling plots.
\end{itemize}

% =========================================================
\section{Approximate cost model}

This section proceeds in four parts. First, I define the variables and assumptions. Second, I present a BSP like, row-by-row sketch of the distributed pipeline to make the supersteps explicit. Third, I derive asymptotic formulas for the cost of each superstep as functions of the number of records \(N\) and the number of nodes \(P\). Finally, I summarize the main observations that connect the model with the measurements.

\subsection{Variables and assumptions}
I use the following symbols and assumptions.

\begin{itemize}
  \item \(N\): number of records. \quad \(P\): number of nodes (one MPI process per node). \quad \(t\): threads per node.
  \item \(F\): input file size in bytes. It depends on keys and payloads.
  \item \(s_I\): size in bytes of one \texttt{IndexRec} entry \(\{\texttt{key},\texttt{offset},\texttt{len}\}\) on this platform.
  \item \(I = N \cdot s_I\): total size in bytes of the in memory index array.
  \item \(B_{\text{read}}\), \(B_{\text{write}}\): effective disk read and write bandwidths.
  \item \(B_{\text{net}}\): effective per process network bandwidth for index transfers.
  \item \(c_s\): constant for local comparison based sorting, so sorting \(M\) keys costs \(c_s\,M\log_2 M\).
  \item \(c_m\): constant for linear merges, so merging two runs of total length \(M\) costs \(c_m\,M\).
\end{itemize}

All times reported in plots exclude the rewrite phase, but I keep a write term in the model for completeness.

\subsection{BSP representation of the program}
The stages are: read and build the global index on rank 0, send index chunks with asynchronous \texttt{Isend}, local sorts on all ranks, receive-and-merge rounds, then the final write on rank 0. For clarity I illustrate \(P=4\).

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    x=0.95cm,y=1.0cm,>=Stealth,
    lane/.style={},
    stage/.style={draw,rounded corners,minimum height=6mm,inner xsep=6pt,fill=gray!10,font=\scriptsize},
    brace/.style={decorate,decoration={brace,amplitude=5pt}},
  ]

    % ---- lanes labels
    \node[anchor=east] at (-0.4,0)    {\small Rank 0};
    \node[anchor=east] at (-0.4,-1.2) {\small Rank 1};
    \node[anchor=east] at (-0.4,-2.4) {\small Rank 2};
    \node[anchor=east] at (-0.4,-3.6) {\small Rank 3};

    % ---- Rank 0: overlap read (upper) and send (lower)
    \node[stage,minimum width=3.0cm] (r0_read) at (1.9, 0.35) {Read + build index};
    \node[stage,minimum width=2.2cm] (r0_sort) at (4.8, 0.35) {Local sort};
    \node[stage,minimum width=3.0cm] (r0_send) at (2.6,-0.35) {Send index chunks};

    % ---- Local sorts: staggered starts (R1 earliest -> R0 latest)
    \node[stage,minimum width=2.2cm] (r1_sort) at (3.6,-1.2) {Local sort};
    \node[stage,minimum width=2.2cm] (r2_sort) at (4.6,-2.4) {Local sort};
    \node[stage,minimum width=2.2cm] (r3_sort) at (5.6,-3.6) {Local sort};

    % ---- Receive-and-merge: first round on ranks 0 and 2 only
    \node[stage,minimum width=2.0cm] (r0_merge1) at (7.3, 0.0) {Rec. + merge};
    \node[stage,minimum width=2.0cm] (r2_merge1) at (8.1,-2.4) {Rec. + merge};

    % ---- Final round on rank 0 only
    \node[stage,minimum width=2.0cm] (r0_merge2) at (10.5,0.0) {Rec. + merge};

    % ---- Final write on rank 0
    \node[stage,minimum width=2.0cm] (r0_write)  at (12.9,0.0) {Write output};

    % ---- Superstep braces (no S0/S1 labels, only named phases)
    % Boundaries chosen to cover the visual extents
    \draw[brace] (0.2,1.0) -- node[above=6pt]{\small Read + Send} (4.3,1.0);
    \draw[brace] (4.3,1.0) -- node[above=6pt]{\small Local sort} (6.8,1.0);
    \draw[brace] (6.8,1.0) -- node[above=6pt]{\small Receive and merge rounds} (11.7,1.0);
    \draw[brace] (11.7,1.0) -- node[above=6pt]{\small Final write} (14.0,1.0);

    % (Arrows intentionally omitted for simplicity)
  \end{tikzpicture}
  \caption{BSP like sketch with \(P=4\). Communication dependencies and timing are not shown with arrows; they are accounted for inside the blocks that perform them.}
  \label{fig:bsp-sketch}
\end{figure}

As the sketch suggests, there are few explicit global barriers. The code encourages overlap and avoids unnecessary waiting:
\begin{enumerate}
  \item Ranks 0, 1, and 2 start their local sorts before rank 3, which is the last one to receive its slice to order.
  \item Pairwise receive and merge steps start as soon as both partner ranks are ready, independently of the other nodes.
\end{enumerate}

The superstep braces \emph{in the upper part of the schema} reflect the critical path and their timing windows:
\begin{itemize}
  \item \textbf{Read + send}: from the start of the read to the end of the last \texttt{Isend}.
  \item \textbf{Local sort}: from the moment when the last rank begins sorting to the moment when the slowest rank finishes sorting.
  \item \textbf{Receive and merge rounds}: from the end of the last local sort to the end of the final merge on rank 0.
\end{itemize}

\subsection{Asymptotic analysis of the computations}
After defining the supersteps along the critical path, I now approximate the cost asymptotically to capture how the program scales with the number of records \(N\) and the number of nodes \(P\).

\paragraph{Read and send.}
Rank 0 reads the file and sends index chunks with nonblocking point-to-point calls (\texttt{MPI\_Isend}). Sends are issued sequentially, so the cumulative send time is proportional to \(I\). Reading and sending overlap partially, so I model the stage as
\[
T_{\text{read+send}}(P)
\;=\;
\max\!\left\{\frac{F}{B_{\text{read}}},\ \frac{I}{B_{\text{net}}}\right\}.
\]

\paragraph{Local sort.}
Each rank sorts about \(N/P\) keys. The cost is
\[
T_{\text{sort}}(P)
\;=\;
c_s\,\frac{N}{P}\,\log_2\!\left(\frac{N}{P}\right).
\]

\paragraph{Multi round merges and transfers.}
Pairwise merging proceeds in \(\lceil\log_2 P\rceil\) rounds. Each round performs a linear merge of total length \(N\) along the critical path, plus transfers of index segments. Ignoring the shrinking message sizes in later rounds gives an upper bound
\[
T_{\text{merge+collect}}(P)
\;=\;
\Bigl(c_m\,N + \frac{I}{B_{\text{net}}}\Bigr)\,\log_2 P.
\]

\paragraph{Final write.}
Rank 0 streams from the unsorted mapping and writes the output:
\[
T_{\text{write}}
\;=\;
\frac{F}{B_{\text{read}}} + \frac{F}{B_{\text{write}}}.
\]

\paragraph{Total.}
The total predicted time is
\[
T_{\text{total}}(P)
\;=\;
T_{\text{read+send}}(P)
\;+\;
T_{\text{sort}}(P)
\;+\;
T_{\text{merge+collect}}(P)
\;+\;
T_{\text{write}}.
\]

\subsection{Observations}
From these expressions I draw the following practical implications:
\begin{itemize}
  \item The dominant computational term is \(c_s\,(N/P)\log_2(N/P)\), the dominant communication term is \(\bigl(c_m N + I/B_{\text{net}}\bigr)\log_2 P\), and the I/O terms are linear in \(F\).
  \item Increasing \(P\) reduces the local sorting cost \(c_s\,(N/P)\log_2(N/P)\), which favors large \(P\) when single node sorting is the bottleneck.
  \item Communication and the number of merge rounds grow with \(\log_2 P\). When the network is slow or per round merges are expensive, small \(P\) is preferable.
  \item Thanks to the \texttt{IndexRec} vector, computation and communication terms are independent of the payload size and scale as \(N\log N\) in \(N\) and as \(\log P\) in \(P\).
  \item The disk bound read and write terms are linear in \(N\) but proportional to the file size \(F\), which grows with the maximum payload, so I/O is sensitive to \(\text{payload}_{\max}\).
\end{itemize}

Overall, the model predicts a clear tradeoff: when per node sorting is slow relative to the interconnect, increasing \(P\) helps, but when communication is slower or dominates, adding nodes hurts; this aligns with my measurements, where few threads per node benefit from distribution while many threads per node see communication bound slowdowns in the plots of Section~5.

\end{document}